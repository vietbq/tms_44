<h1><%= t "admin.subject.all" %></h1>
<br>
<br>
<%= will_paginate %>
<table class="table table-hover"> 
  <tbody>
    <tr> 
      <th>#</th> 
      <th><%= t "name" %></th> 
      <th><%= t "description" %></th> 
      <th><%= t "created_at" %></th> 
      <th></th>
    </tr>
    <ul class="subjects">
      <%= render @subjects %>
    </ul>
  </tbody>
</table>
<%= will_paginate %>
