<nav class="navbar navbar-admin">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse"
        data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only"><%= t "header.nav" %></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to t("header.brand"), admin_root_path, class: "navbar-brand" %>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if logged_in_superuser? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <%= t "user.account" %> <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to t("user.profile"), admin_superuser_path(current_superuser) %></li>
              <li>
                <%= link_to t("user.setting"), edit_admin_superuser_path(current_superuser) %>
              </li>
              <li class="divider"></li>
              <li>
                <%= link_to t("user.logout"), admin_logout_path, method: "delete" %>
              </li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to t("user.login"), admin_login_path %></li>
        <% end %>
    </div>
  </div>
</nav>
